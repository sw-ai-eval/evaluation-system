<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eval.domain.multi.mapper.MultiMapper">

	<select id="findMultiEval" parameterType="map" resultType="com.eval.domain.multi.dto.MultiEvalDTO">
        SELECT 
            etm.id, 
            etm.evaluator_no AS evaluatorNo, 
            etm.evaluatee_no AS evaluateeNo, 
            et.name AS evalName, 
            etm.step, 
            etm.status
        FROM eval_target_mapping_52 etm
        JOIN eval_type_52 et ON etm.eval_type_id = et.id
        WHERE et.name LIKE '%다면평가%' 여기서 다면평가 관련 필터
        <if test="position == '부서원'">
            AND etm.evaluator_no = #{userNo}
        </if>
        <if test="position == '부서장'">
            AND etm.evaluatee_no = #{userNo}
        </if>
    </select>
    

</mapper> -->